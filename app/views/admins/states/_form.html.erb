<%= form_for(admins_state) do |f| %>
  <% if admins_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_state.errors.count, "error") %> prohibited this admins_state from being saved:</h2>

      <ul>
      <% admins_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :abbreviation %>
    <%= f.text_field :abbreviation %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

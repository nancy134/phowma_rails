<%= form_for(admins_zip) do |f| %>
  <% if admins_zip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_zip.errors.count, "error") %> prohibited this admins_zip from being saved:</h2>

      <ul>
      <% admins_zip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %>
    <%= f.number_field :code %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

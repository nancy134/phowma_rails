<%= form_for(admins_post) do |f| %>
  <% if admins_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_post.errors.count, "error") %> prohibited this admins_post from being saved:</h2>

      <ul>
      <% admins_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %>
    <%= f.text_field :message %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
